{
  "name": "erikdrouhard-2026-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check:type": "node scripts/check-type-system.mjs",
    "sync:resume": "node scripts/sync-resume.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
