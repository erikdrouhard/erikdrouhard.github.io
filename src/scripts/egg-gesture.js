/* ==========================================================================
   egg-gesture.js — the pull-from-the-pocket gesture and the sheet springs.

   Filled by ticket 04. It exists now so egg.js can be written against the
   seam it will use: initGesture() receives the already-queried elements and
   the sheet API, so the gesture never has to look at the DOM twice or reach
   back into egg.js for open/close.
   ========================================================================== */

// eslint-disable-next-line no-unused-vars
export function initGesture(_els, _api) {}

export function stopGesture() {}
